# JSON Forge 实现任务

## 目标

构建一款默认离线、轻量且高性能的跨平台 JSON 桌面工具，首发支持 macOS 和 Windows。首版聚焦高频 JSON 工作流，不接入账号、云同步或必须联网的 AI 服务。

## 任务清单

[x] 初始化 Tauri 2 + React + TypeScript + Vite 工程 | P0 | 1h | 无

[x] 建立设计令牌、应用外壳、响应式工作台和明暗主题 | P0 | 2h | 初始化工程

[x] 实现多标签文档模型、自动恢复、脏状态与关闭确认 | P0 | 2h | 初始化工程

[x] 集成 CodeMirror 6 编辑器、JSON 高亮、括号匹配、搜索替换和快捷键 | P0 | 2h | 应用外壳

[x] 实现 Web Worker 处理管线：校验、格式化、压缩、递归键排序和错误定位 | P0 | 3h | 文档模型、编辑器

[x] 实现格式修复、字符串转义/反转义和结果覆盖/新标签输出 | P0 | 2h | 处理管线

[x] 实现树形视图、节点展开折叠、路径复制和文本/树视图切换 | P0 | 3h | 处理管线

[x] 实现双栏 JSON Diff、差异统计和左右内容交换 | P0 | 3h | 文档模型、编辑器

[x] 接入 Tauri 原生打开/保存、拖放文件、剪贴板和最近文件 | P0 | 3h | 文档模型

[x] 完成工具栏、命令面板、状态栏、设置面板及完整键盘操作 | P0 | 3h | 核心功能

[x] 增加单元测试、组件测试和关键状态持久化测试 | P0 | 3h | 核心功能

[x] 完成 Playwright 桌面/窄窗口视觉验收及主要操作流验收 | P0 | 2h | 核心功能

[x] 配置 macOS/Windows 构建、GitHub Actions 和安装包产物 | P0 | 2h | 初始化工程

[x] 补充 README、开发命令、快捷键和发布说明 | P1 | 1h | 构建配置

[ ] 评估 JSON/YAML/XML/TOML 与类型生成器扩展 | P2 | 待定 | 首版完成

[ ] 评估可选 AI 修复接口，确保密钥仅存本地且功能可完全关闭 | P2 | 待定 | 首版完成

## 首版验收标准

- macOS 可在本机完成 `.dmg` 或 `.app` 构建；Windows 构建配置可在 CI 生成 `.msi` 或 NSIS 安装包。
- 首屏直接进入 JSON 工作台，不展示营销页、不要求登录，核心处理无需联网。
- 支持创建、打开、拖入、编辑、保存和恢复多个 JSON 标签页，并正确提示未保存修改。
- 格式化、压缩、键排序、修复、转义和反转义均可撤销，失败时保留原文。
- 非法 JSON 在编辑器与状态栏显示可操作的行列错误；动态提示可被辅助技术读取。
- 文本、树形和 Diff 工作流均可只用键盘完成主要操作。
- 10 MB JSON 的格式化、压缩和校验不阻塞主界面；处理期间提供可取消的明确状态。
- 1280x800、1440x900、1024x768 和 375x812 视口不出现工具栏文字溢出或不可恢复的界面遮挡。
- 明暗主题均满足正文最低 4.5:1 对比度，且遵循系统主题与 `prefers-reduced-motion`。

## 非目标

- 首版不提供在线分享、登录、云同步、团队空间或远程 API 托管。
- 首版不承诺超大文件流式编辑；超过建议阈值时进入受限模式并明确提示。
- 首版不内置需要网络或付费模型才能工作的 AI 功能。
